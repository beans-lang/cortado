<HStack align="center">
  <Box width={self.thumb_leading} height={3} background={self.accent} corner_radius={self.capsule} />
  <Box width={14} height={14} background={self.knob} corner_radius={self.capsule}
       border_width={1} border_color={self.track_off} />
  <Box grow={1} height={3} background={self.track_off} corner_radius={self.capsule} />
</HStack>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class SliderTemplate extends component.RangeControlTemplate {
    pub fn init() { super.init() }
}
</beans>
