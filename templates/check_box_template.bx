<HStack spacing={8} align="center">
  <Label text={self.mark} text_color={self.on_accent} background={self.track}
         corner_radius={self.radius_small} width={20} height={20} />
  <Label text={self.title} text_color={self.ink} font_size={self.font_size} />
</HStack>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class CheckBoxTemplate extends component.ToggleControlTemplate {
    pub fn init() { super.init() }
}
</beans>
