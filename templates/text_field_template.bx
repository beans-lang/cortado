<VStack background={self.card} corner_radius={self.radius_small}
        border_width={1} border_color={if self.focused { self.accent } else { self.separator }} />
<beans>
package templates

import cortado.component
import {view} from cortado.annotations

@view
pub partial class TextFieldTemplate extends component.ControlTemplate {
    pub fn init() { super.init() }
}
</beans>
