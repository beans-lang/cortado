<VStack background={self.card} corner_radius={self.radius_medium}
        border_width={if self.focused { 2.0 } else { 1.0 }}
        border_color={if self.focused { self.accent } else { self.hairline }} />
<beans>
package templates

import cortado.component
import {view} from cortado.annotations

@view
pub partial class TextFieldTemplate extends component.ControlTemplate {
    pub fn init() { super.init() }
}
</beans>
