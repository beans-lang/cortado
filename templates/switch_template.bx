<Box background={if self.checked { self.accent_fill } else { self.track_off }}
     corner_radius={self.capsule} width_percent={100.0} height_percent={100.0}>
  <Rectangle x={if self.checked { self.switch_width - self.switch_inset - self.switch_knob_width } else { self.switch_inset }}
             y={self.switch_inset}
             width={self.switch_knob_width} height={self.switch_knob_height}
             corner_radius={self.switch_knob_height / 2.0}
             fill={self.knob}
             shadow_color={self.knob_shadow} shadow_blur={1.5} shadow_dy={0.5} />
</Box>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class SwitchTemplate extends component.ToggleControlTemplate {
    pub fn init() { super.init() }
}
</beans>
