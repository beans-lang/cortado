<VStack background={self.card} corner_radius={self.radius_medium} padding={12} align="stretch">
  <Label text={self.title} text_color={self.muted} font_size={self.footnote} />
</VStack>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class GroupBoxTemplate extends component.ControlTemplate {
    pub fn init() { super.init() }
}
</beans>
