<VStack background={self.fill} corner_radius={self.radius} padding={3}
        border_width={1} border_color={if self.focused { self.accent } else { self.separator }}
        align="center" justify="center">
  <Label text={self.title} text_color={self.ink} font_size={self.font_size} alignment={1} />
</VStack>
<beans>
package templates

import cortado.component
import {view} from cortado.annotations

@view
pub partial class ButtonTemplate extends component.ControlTemplate {
    pub fn init() { super.init() }
}
</beans>
