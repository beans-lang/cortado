<HStack align="center">
  <Box width_percent={self.percent} height={5} background={self.accent} corner_radius={self.radius_small} />
  <Box grow={1} height={5} background={self.track_off} corner_radius={self.radius_small} />
</HStack>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class LevelIndicatorTemplate extends component.RangeControlTemplate {
    pub fn init() { super.init() }
}
</beans>
