<HStack background={self.track_off} corner_radius={self.capsule} align="stretch">
  <Box width_percent={self.percent} background={self.accent} corner_radius={self.capsule} />
  <Box grow={1} />
</HStack>
<beans>
package templates
import cortado.component
import {view} from cortado.annotations
@view
pub partial class LevelIndicatorTemplate extends component.RangeControlTemplate {
    pub fn init() { super.init() }
}
</beans>
