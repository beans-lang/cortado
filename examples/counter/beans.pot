module counter
kind application

# A component-driven window. It needs three modules: cortado for the framework,
# cortado_app for the composition root that wires barista in, and barista
# because this file names `ServiceCollection` itself.
require path "../.."
require path "../../app"
require github.com/beans-lang/barista v0.1.1
